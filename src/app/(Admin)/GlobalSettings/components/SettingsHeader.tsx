"use client";

import { PageHeader } from "@/components/PageHeader";

export function SettingsHeader() {
  return <PageHeader title="Global Settings" subtitle="Platform-wide rules applied to every evaluation." />;
}
